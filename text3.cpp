#include <stdio.h>



int main(void) {
    char buffer[100] = {0};
    int n, len_max;
    for(int i = 1; i <= n; i++) {
        gets(buffer);
        int const len = strlen(buffer);


    }
    gets(buffer);
    const int len = 10;
    char *text[len] = {0};


}
